name: Verify and Publish Plugin

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      force_publish:
        description: Force publish the current ref to Marketplace Stable
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    outputs:
      version_changed: ${{ steps.version.outputs.version_changed }}
      old_version: ${{ steps.version.outputs.old_version }}
      new_version: ${{ steps.version.outputs.new_version }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Detect plugin version change
        id: version
        shell: bash
        env:
          BEFORE_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail

          extract_version() {
            awk -F'=' '/^[[:space:]]*pluginVersion[[:space:]]*=/{gsub(/^[[:space:]]+|[[:space:]]+$/, "", $2); print $2; exit}' "$1"
          }

          new_version="$(extract_version gradle.properties)"
          old_version=""
          version_changed="false"

          if [[ "${GITHUB_EVENT_NAME}" == "push" ]] && [[ -n "${BEFORE_SHA:-}" ]] && [[ "${BEFORE_SHA}" != "0000000000000000000000000000000000000000" ]]; then
            tmp_file="$(mktemp)"
            if git show "${BEFORE_SHA}:gradle.properties" > "${tmp_file}" 2>/dev/null; then
              old_version="$(extract_version "${tmp_file}")"
              if [[ -n "${old_version}" ]] && [[ "${old_version}" != "${new_version}" ]]; then
                version_changed="true"
              fi
            fi
            rm -f "${tmp_file}"
          fi

          echo "old_version=${old_version}" >> "${GITHUB_OUTPUT}"
          echo "new_version=${new_version}" >> "${GITHUB_OUTPUT}"
          echo "version_changed=${version_changed}" >> "${GITHUB_OUTPUT}"

          echo "Old version: ${old_version:-<none>}"
          echo "New version: ${new_version}"
          echo "Version changed: ${version_changed}"

      - name: Require plugin metadata update on release pushes
        if: github.event_name == 'push' && steps.version.outputs.version_changed == 'true'
        shell: bash
        env:
          BEFORE_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail
          if ! git diff --name-only "${BEFORE_SHA}" "${GITHUB_SHA}" | grep -Fxq "src/main/resources/META-INF/plugin.xml"; then
            echo "::error::pluginVersion changed, but src/main/resources/META-INF/plugin.xml was not updated."
            exit 1
          fi

      - name: Run tests and build plugin
        run: ./gradlew --stacktrace test buildPlugin

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-${{ steps.version.outputs.new_version }}
          path: build/distributions/*.zip
          if-no-files-found: error

  publish:
    needs: verify
    if: needs.verify.result == 'success' && (needs.verify.outputs.version_changed == 'true' || (github.event_name == 'workflow_dispatch' && github.event.inputs.force_publish == 'true'))
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Ensure Marketplace token is configured
        shell: bash
        env:
          JB_MARKETPLACE_TOKEN: ${{ secrets.JB_MARKETPLACE_TOKEN }}
        run: |
          set -euo pipefail
          if [[ -z "${JB_MARKETPLACE_TOKEN:-}" ]]; then
            echo "::error::Missing GitHub secret JB_MARKETPLACE_TOKEN."
            exit 1
          fi

      - name: Publish plugin to Marketplace Stable
        env:
          JB_MARKETPLACE_TOKEN: ${{ secrets.JB_MARKETPLACE_TOKEN }}
        run: ./gradlew --stacktrace publishPlugin
